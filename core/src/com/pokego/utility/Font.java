package com.pokego.utility;

/**
 * Defines the available fonts in the game. To generate new fonts, add the .fnt file to the "./assets/fonts" folder and add the respective enum in.
 */
public enum Font {
    VERDANA,
    LUCIDA_SANS,
    NONE
}
